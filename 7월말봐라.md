# 📅 7월말 봐라 — 레이더 튜닝 체크포인트 (2026-07-10 전면 갱신판)

> 최초 작성: 2026-06-27 · **전면 갱신: 2026-07-10 (정렬4 rank_bucket 개편 반영)** · 읽는 시점: **2026-07-25 전후**
> 한 줄 요약: **rank_bucket 전진검증 표본이 쌓였는지 확인하고 → 쌓였으면 Claude에게 "7월말봐라.md대로 정렬4 채점하자"라고 말하면 된다.**

⚠️ 구판 주의: 6/27 원본이 가리키던 `/Users/jinjin/stocknews`·`agent_alpha`는 **은퇴/삭제됐다(7/6)**. 현재 저장소는 `/Users/jinjin/kiwoomnews`, 검증 데이터는 `web/data/performance.json`의 `rank_bucket_stats`다. 구판의 복붙 명령은 쓰지 마라(문서 맨 아래 부록으로 이동).

---

## 0. 배경 30초 — 7/10에 무엇을 했나

2026-07-10에 suspects 정렬을 **정렬4 rank_bucket 체계로 전면 개편**했다(커밋 7f3acab, 합의 계보: 순위분석codex → 순위재정렬2 → 정렬1 → 정렬2 → 정렬3 → **정렬4.md(SSOT)**).

- **버킷 순위**: 매우좋음 Tier1·Tier2(a안) → 급소+회전150 → 저점매집+회전90 → 저점매집 → 흔들기(조합D+75 → 75 → 조합D) → 75점 기타 → 흔들기 기타 → 규제해소 → 급소 단독 → 기타. **매우좋음후보 승격키는 제거**(익일 종가 −2.3%·상승마감 17% 실측).
- 매 게시마다 suspect·history에 `rank_bucket`·`rank_reason`·`shadow_bucket`·`expected_touch7_rate`·`expected_high_pct` 저장, 17:20 backtest가 `rank_bucket_stats`(버킷별 상호배타 채점 + kill switch 자동판정)를 만든다. `/performance`에 표가 뜬다.
- 같은 날 **투자위험 해제 직후 승격**(`alert_risk_released`, 서산 원형)과 **경고 경과일 기록**(`alert_elapsed_days`) 추가. 7/9엔 **뉴스·공시 재료등급**(S/A/B/C/D/N, `material_bands`) 추가.
- 근거는 6/10~7/9 표본 179건 — **약세장 단일 레짐·일부 버킷 n<10**이라, 7월말 전진 표본으로 재판정하기로 합의했다. 이 파일이 그 약속이다.

---

## 1. ✅ 먼저 확인: 전진 표본이 쌓였나 (복붙 1줄)

```bash
cd /Users/jinjin/kiwoomnews && python3 -c "
import json
d = json.load(open('web/data/performance.json'))['rank_bucket_stats']
print('bucket | n / 종목 / +7터치 / 하한 / 종가평균')
for c in d['cells']:
    print(f\"{c['bucket']:>2} {c['band']}: n={c['n']} 종목{c['unique_n']} 터치{c['touch7_rate']} 하한{c['wilson7_lower']} 종가{c['avg_return']}\")
for k in d['kill_switches']:
    print('KS', k['key'], k['status'], k['reasons'])
"
```

**판단** (7/10 시점 대비 — 당시 bucket1 n=5, bucket2 n=14, bucket4 n=8):
- 상위 버킷(1·2·3·4·5)에 **7/11 이후 신규 표본이 각 5건 이상** 추가됐으면 → 🟢 2번으로.
- 거의 안 늘었으면 → 🟡 표본이 안 쌓인 것(빈 레이더가 많았거나 cron 문제 — `tail /tmp/kiwoom_publish.log`·`/tmp/kiwoom_backtest.log` 확인). 1~2주 더 기다려라.
- **KS(kill switch)가 "하향상신"이면 기다리지 말고 즉시 Claude에게 보고받아라** — 특히 bucket1(급소+회전150)은 실패 1건이면 자동 상신되게 돼 있다.

---

## 2. 🟢 표본이 쌓였으면: Claude에게 이렇게 말해라

> **"7월말봐라.md대로 정렬4 채점하자. rank_bucket_stats 분석해서 승격·강등·문턱 조정 근거 보여주고 제안해줘."**

### Claude 실행 체크리스트 (Claude용 지침)

1. **버킷 채점** — `rank_bucket_stats`의 셀별(상호배타 기준) n·고유종목·터치·윌슨하한·종가평균·상승마감을 **7/10 스냅샷(`radar.py`의 `RANK_BUCKET_BASELINES`)과 비교**. 전진 표본에서 무너진 버킷과 강해진 버킷을 표로.
2. **킬스위치·승격 판정** — 사전 고정 기준 그대로: 승격 = n≥20 AND 고유종목≥10 AND 윌슨하한≥60% AND 종가평균>+3% (shadow S1~S5 심사). 강등 = 정렬4.md §킬스위치. **기준 사후 조정 금지.**
3. **75 vs 80 문턱 검증 (7/10 회장님 가설)** — "점수 차이가 승률을 가른다": 당시 실측은 흔들기 내 score 80+ 터치 88% vs 75~79 75% vs 70~74 67%(교차 셀 n=1~8이라 미확정). 전진 표본으로 흔들기 버킷(4·5)의 문턱을 75→80으로 올렸을 때 채점이 개선되는지 비교표 제시. 씨이랩(77점, 조합C, 기대 +17.39% → 실제 +5.79%)이 원형 사례.
4. **상단컷 할인 검증 (7/10 씨이랩 교훈)** — `상단컷` 배지(run_6d≥30% 또는 재료 C/N+회전90%+) 동반 표본 vs 미동반 표본의 기대치 대비 실제 고가 괴리를 비교. 유의하면 "상단컷 시 expected 하향 표기"를 제안(정렬 불변, 표시만).
5. **재료등급 검증** — `material_bands`·`material_signal_bands`(valid 셀만). ⚠ 등급 로직이 7/9에 3회 바뀌어 **7/10 이후 표본만 신뢰**. S/A 조합의 리프트가 실재하면 그때 정렬 반영을 논의.
6. **경보 코호트 검증** — `alert_elapsed_days`별(지정 첫날 vs 경과) 성과와 `alert_risk_released`(위험 해제 직후, 서산 7/10 원형) 표본 채점. shadow S2(경고+70점)는 고유종목≥10 전까지 승격 금지.
7. **반드시 지킬 것** — 자동튜닝 X, 근거 제시 후 회장님 수동 합의. 적용 전 before/after suspects 순서 diff 제시. 변경은 롤백 1커밋으로 가능하게. 자동매매 비중은 별도 합의 전 불변.

---

## 3. 🟡 문제해결 (표본이 안 쌓였을 때)

```bash
cd /Users/jinjin/kiwoomnews
tail -5 /tmp/kiwoom_publish.log      # 10분 게시 cron이 돌았나
tail -5 /tmp/kiwoom_backtest.log     # 17:20 채점 cron이 돌았나
crontab -l | grep -c kiwoomnews      # cron 설치 확인
```

- cron이 없으면 `bash scripts/install_cron.sh` 재실행. Mac이 평일 9~20시에 깨어 있어야 한다.
- 수상종목 0건인 날이 많았던 것이면 정상(빈 레이더도 유효) — 그냥 더 기다려라.

---

## 4. 참고 — 7/10 기준선 (전진 표본과 비교할 원본 숫자)

**상호배타 소급 채점(179건, 6/10~7/9)** — 코호트(겹침 포함) 수치와 다르다. 전진 비교는 이 표 기준:

| bucket | n | 종목 | +7터치 | 하한 | 종가평균 | 메모 |
|---|---|---|---|---|---|---|
| 0 매우좋음 | 2 | 2 | 100% | 34 | +19.3 | Tier2 표본 0건 (a안으로 포함시킴) |
| 1 급소+회전150 | 5 | 3 | 100% | 57 | +17.7 | 킬스위치 1순위 감시 대상 |
| 2 저점매집+회전90 | 14 | 12 | 85.7% | 60 | +7.6 | 가장 건강 |
| 3 저점매집 기타 | 6 | 5 | 66.7% | 30 | +1.5 | 겹침 빼면 약함 — 강등 후보 감시 |
| 4 흔들기+조합D+75 | 8 | 8 | 87.5% | 53 | +2.6 | |
| 5 흔들기+75 | 2 | 2 | 50% | 10 | −5.1 | 잔여 표본 극소 — 씨이랩 포함 |
| 7 75점 기타 | 25 | 19 | 72% | 52 | −0.4 | |
| 8 흔들기 기타 | 8 | 8 | 75% | 41 | −2.5 | |
| 10 급소 단독 | 5 | 5 | 20% | 4 | −6.8 | 겹침 빼면 최약 — 강등 후보 감시 |
| 11 기타 | 98 | 50 | 27.6% | 20 | −0.8 | |

**7/10 당일 실증 교훈**(0710예측.md 채점): 소표본 명목 확률은 과대(예측 터치 76% → 실제 57%) / 재료 C·N + 고회전은 하향(흥구석유 −13.6%p 오차) / run6 30%+는 상단컷(씨이랩 재료 S에도 +5.8%) / 고가 터치 ≠ 실현수익(7종목 전부 고가 후 되밀림).

관련 문서: `정렬4.md`(SSOT) · `순위재정렬2.md`·`정렬1~3.md`(합의 계보) · `0710예측.md`(예측·채점 원형) · `0710수정.md`(7/10 수정 이력).

---

## 부록 — 6/27 구판 내용 (참고용, 실행 금지)

구판은 `/Users/jinjin/stocknews`의 agent_alpha calibration(스파크·키움집중·외인매집 교차신호, total_labeled≥20 기준)과 change_band 가점 튜닝을 지시했으나, **alpha는 7/6 삭제·저장소는 kiwoomnews로 이관**됐다. 당시 근거였던 change_band(등락률 낮을수록 익일↑, n=92) 신호 자체는 여전히 `change_band_stats`로 쌓이고 있으니 2번 채점 시 함께 보라. 구판 상세 플랜: `~/.claude/plans/twinkling-cooking-stroustrup.md`.
